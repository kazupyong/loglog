<%= form_for(@administrators_log) do |f| %>
  <% if @administrators_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@administrators_log.errors.count, "error") %> prohibited this administrators_log from being saved:</h2>

      <ul>
      <% @administrators_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
